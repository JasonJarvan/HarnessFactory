---
last_reviewed_at: <TODO: YYYY-MM-DD>
---

# 版本计划

> 多版本路线图。当前版本写详细（目标 + 待办），下一版写方向 + 主要工作项，
> 再往后只列方向，最远只放「待验证 / 可能扩展」。这是仓库长期层的事实源，
> 不是某个任务的临时文档。

## 当前定位

<TODO: 项目当前所处的整体阶段、目标用户、与上下游的关系。一到两段话。>

## v0.1 目标

- <TODO: v0.1 要达成的核心目标，3–6 条；每条用一句话写清楚要交付什么>

## v0.1 待办

- <TODO: 为达成 v0.1 目标必须完成的工作项，按优先级排列；细到能切成 OpenSpec change 的颗粒度>

## v0.2 目标

- <TODO: 紧随 v0.1 之后的版本目标；可比 v0.1 略粗一些>

## v0.2 待办

- <TODO: v0.2 主要工作项；粗粒度可，等 v0.1 收尾再细化>

## v0.3 方向

- <TODO: 再下一版的方向，可只列条目，不必详细>

## 未来考虑

- <TODO: 更远的设想 / 待验证的假设 / 可能的扩展。允许写不确定项>

## 维护规则

- 当前版本完成（OpenSpec.archive + RepoMem.merge 走完）后，把当前段落归档进
  `architecture/` 或 `memory/`（如果产生了稳定知识），并把下一版从「目标」
  晋升为「当前」。
- 路线图变化（例如新增一个版本目标、推迟某条目）属于长期层变更，更新
  `last_reviewed_at`。
